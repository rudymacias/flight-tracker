export interface Flight {
    flightNumber: string;
    origin: string;
    destination: string;
    departureTime: string;
    arrivalTime: string;
    originTimezone: string;
    destinationTimezone: string;
}
